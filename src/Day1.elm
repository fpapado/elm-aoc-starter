module Day1 exposing (part1)


part1 : String -> String
part1 input =
    "Here is where solutions go. For convenience, we return a String, so we can display them in the CLI.\nYou could use Debug.toString in a pinch, or make a custom string representation.\nHappy coding!"
